package com.skillsphere.repository;

// This file is intentionally left empty. Repository interfaces have been
// split into their own public files (e.g. UserRepository.java) to allow
// use from other packages. Keeping this placeholder avoids build errors
// if any code still references Repositories.java indirectly.
